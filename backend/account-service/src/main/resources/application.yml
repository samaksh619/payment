server:
  error:
    include-message: always
  port: 8082
spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/payflow}
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
