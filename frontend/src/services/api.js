const BASE='http://localhost:8080/api';
export async function api(path,options={}){const token=localStorage.getItem('token');const headers={'Content-Type':'application/json',...(options.headers||{})};if(token)headers.Authorization='Bearer '+token;const r=await fetch(BASE+path,{...options,headers});if(!r.ok){if(r.status===401){localStorage.removeItem('token');localStorage.removeItem('user');if(location.pathname!=='/'){location.href='/'}throw new Error('Session expired, please sign in again')}const t=await r.text();let msg=t;try{const j=JSON.parse(t);msg=j.message||j.error||t}catch(_){}throw new Error(msg||'Request failed')}return r.status===204?null:r.json();}
